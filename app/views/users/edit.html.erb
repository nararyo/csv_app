<h1>編集画面</h1>

<%= form_for @user do |f| %>
  <p>投稿する</p>
  <p>名前　<%= f.text_field :name %></p>
  <p>年齢 <%= f.text_field :age %></p>
  <p>出身　<%= f.text_field :address %></p>
  <input type="submit" value="send">
<% end %>